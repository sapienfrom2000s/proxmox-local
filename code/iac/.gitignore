# Provider plugin cache (platform-specific, ~50MB)
.terraform/

# Crash logs
crash.log

# Variable files (contain secrets)
*.tfvars
!terraform.tfvars.example

# Override files as they're user-specific
override.tf
override.tf.json
*_override.tf
*_override.tf.json
